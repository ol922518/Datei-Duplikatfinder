# Erforderlich für die Oberfläche (PySide6/Qt) - bringt auch natives
# Drag & Drop mit (kein Zusatzpaket mehr nötig).
PySide6>=6.7.0

# Geteilte UI-Bausteine (FlowLayout, TitledFrame, InfoIcon, ...) - liegen als
# eigenes, lokal editierbares Paket im Geschwister-Ordner ../qt-app-kit
# (siehe dortige README). Muss daher als Ordner neben Datei-Duplikatfinder/
# liegen.
-e ../qt-app-kit

# Für den exakten Duplikat-Vergleich (Hashing über die Python-Standardbibliothek
# hashlib) wird kein weiteres Paket benötigt.

# Optional – ohne diese Pakete funktioniert die App trotzdem, nur die
# jeweilige Funktion ist dann eingeschränkt:
Pillow>=10.0.0      # "Ähnliche Bilder erkennen" (Perceptual Hashing) + Foto-Metadaten im Datei-Viewer
python-docx>=1.1.0  # Word-Dateien (.docx) als Text im Datei-Viewer anzeigen
python-pptx>=1.0.0  # PowerPoint-Dateien (.pptx) als Text je Folie im Datei-Viewer anzeigen (keine Bilder/Layout)
send2trash>=1.8.0   # "🗑 Markierte Zeilen löschen" - verschiebt in den Papierkorb statt endgültig zu löschen (plattformübergreifend)

# PDF- und Bild-Vorschau im Datei-Viewer brauchen kein weiteres Paket -
# QtPdf/QtPdfWidgets sind bereits in PySide6 enthalten.

# Nur macOS: sorgt dafür, dass ein zweiter Doppelklick auf das App-Bundle bei
# bereits laufender App wirklich das bestehende Fenster vor andere Apps holt
# (window.raise_()/activateWindow() von Qt allein reicht dafür nicht, wenn
# gerade eine andere App aktiv ist - siehe qt_app_kit/single_instance.py,
# wire_single_instance_server()).
# Ohne dieses Paket läuft der Einzelinstanz-Mechanismus trotzdem, holt das
# Fenster dann aber ggf. nicht zuverlässig nach vorne.
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
